{% extends 'base.html.twig' %}

{% block title %}Information for product {{ product.id }}{% endblock %}

{% block body %}
    <div class="jumbotron">
        <a href="{{ path('home') }}">Back to the home</a>
        <h1>{{ product.name }}</h1>
        <p class="lead">{{ product.description }}</p>
        <p>Price: {{ product.price }} $</p>
    </div>
{% endblock %}